knp_paginator:
  page_range: 5                     # Nombre de pages affichées dans la barre de pagination
  default_options:
    page_name: page # Nom du paramètre de la page
    sort_field_name: sort
    sort_direction_name: direction
    distinct: true
  template:
    pagination: '@KnpPaginator/Pagination/bootstrap_v5_pagination.html.twig' # Modèle Bootstrap 5
