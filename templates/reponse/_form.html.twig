{{ form_start(form) }}
<div class="form-group">
    {# Render label for 'Message' field #}
    {{ form_label(form.Message) }}

    {# Render input widget (field) for 'Message' with Bootstrap styling #}
    {{ form_widget(form.Message, {'attr': {'class': 'form-control'}}) }}

    {# Render any validation errors for the 'Message' field #}
    {{ form_errors(form.Message) }}
</div>

{# Render submit button to save the form #}
<button type="submit" class="btn btn-primary">Save</button>

{{ form_end(form) }}
