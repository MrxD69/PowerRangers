{% extends 'base.html.twig' %}

{% block title %}Edit Offer{% endblock %}

{% block body %}
    <div class="container my-5">
        <h1 class="text-center mb-4">Edit Offer</h1>

        <div class="card border-primary shadow-sm p-4">
            <div class="card-body">
                {{ include('commande_db/_form.html.twig', {'button_label': 'Update'}) }}
            </div>
        </div>

        <div class="d-flex justify-content-between align-items-center mt-4">
            <a href="{{ path('app_commande_db_index') }}" class="btn btn-secondary">
                <i class="bi bi-arrow-left"></i> Back to List
            </a>
            {{ include('commande_db/_delete_form.html.twig', { 'class': 'd-inline' }) | raw }}
        </div>
    </div>
{% endblock %}
