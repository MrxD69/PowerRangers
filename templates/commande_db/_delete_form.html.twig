<form method="post" action="{{ path('app_commande_db_delete', {'id': commande_db.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commande_db.id) }}">
    <button class="btn btn-danger">
        <i class="bi bi-trash"></i> Delete
    </button>
</form>